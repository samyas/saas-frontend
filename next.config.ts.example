# =============================================================================
# Next.js Configuration for Containerized Deployment
# Add this to your existing next.config.ts or replace if minimal
# =============================================================================

import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Enable standalone output for Docker deployment
  // This creates a minimal production bundle with only necessary files
  output: 'standalone',

  // Disable x-powered-by header for security
  poweredByHeader: false,

  // Enable React strict mode for better development experience
  reactStrictMode: true,

  // Configure remote image domains if needed
  images: {
    remotePatterns: [
      // Add your image domains here
      // {
      //   protocol: 'https',
      //   hostname: 'example.com',
      // },
    ],
  },

  // Environment variables available at build time
  // Runtime env vars should use NEXT_PUBLIC_ prefix
  env: {
    // Add build-time env vars here if needed
  },
};

export default nextConfig;
